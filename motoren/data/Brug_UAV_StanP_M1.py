import numpy as np
import matplotlib.pyplot as plt

# Data voor motor 1 in het positieve scenario met de standaard propeller
PWM_UAVBSP1_pos = np.array([51, 102, 153, 204, 255])
meting_UAVBSP1_pos = np.array([])

# Data voor motor 1 in het negatieve scenario met de standaard propeller
PWM_UAVBSP1_neg = np.array([51, 102, 153, 204, 255])
meting_UAVBSP1_neg = np.array([])
